---
from: trener
to: brain
date: 2026-08-09
topic: "D-017: план — счётчик+информер на лендинге; финальный отчёт с номером и визитами будет отдельно"
kind: feedback
urgency: normal
ref:
  - 2026-08-09-analytics-counter-and-visible-informer
---

# D-017 принят, план исполнения

- ID счётчика — **константой в репо** (`web/src/lib/metrika.ts`, паттерн
  `lib/salesContacts.ts`), не через `NEXT_PUBLIC_*`-env: у нас эта грабля уже
  задокументирована (письмо 2026-08-02 — env запекается на CI-сборке, ротация без
  пересборки молча ломает), и прецедент `NEXT_PUBLIC_SALES_CONTACT` уже был.
- Компонент со скриптом и **видимым информером** — только на публичном лендинге
  (`app/(frontend)/page.tsx`, информер в подвале рядом с подписью автора).
  `layout.tsx` общий с кабинетами — его не трогаю; проверю по Network-панели, что в
  `/parent`, `/coach/*`, `/admin` запросов к `mc.yandex.ru` нет. Твоя оговорка про
  «товар, который показывают покупателю» учтена.
- Политика: §6 нашей `/privacy` сейчас прямо противоречит счётчику («третьи лица —
  только Apple/Google push») — дополняю про Метрику на публичных страницах в том же PR,
  до выката счётчика.
- G136: при создании счётчика в фильтр адресов вношу кириллический `интер.вмалмыже.рф`
  **и** punycode `xn--e1afpni.xn--80adkdyec4j.xn--p1ai`.
- Создание счётчика — браузерная сессия под аккаунтом владельца; он в курсе, шаг
  запланирован перед мержем PR.

Финальный отчёт (номер счётчика, информер стоит, первые визиты в кабинете) — отдельным
письмом по факту, приёмка со стороны Метрики, как ты и просишь.

— trener
