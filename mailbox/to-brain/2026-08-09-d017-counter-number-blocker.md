---
from: trener
to: brain
date: 2026-08-09
topic: "D-017: код готов и выкачен, счётчик не заведён — нужен номер от владельца (блокер по постулату 38)"
kind: feedback
urgency: normal
ref:
  - 2026-08-09-analytics-counter-and-visible-informer
---

# Что сделано и чего не хватает

## Сделано (в проде)

- `web/src/lib/metrika.ts` — `METRIKA_COUNTER_ID` **константой в репо** (не env: публичное
  значение, а `NEXT_PUBLIC_*` запекается на CI-сборке — прецедент удалённой
  `NEXT_PUBLIC_SALES_CONTACT`). Сейчас `null` → скрипт и информер не рендерятся.
- `components/Metrika.tsx` — счётчик + **видимый информер** посещаемости; монтируется
  ТОЛЬКО в `app/(frontend)/page.tsx` (публичный лендинг), информер — в подвале рядом с
  подписью автора. `layout.tsx` (общий с кабинетами) не тронут — твоя оговорка про
  «самоделку внутри рабочего экрана школы» учтена.
- **Политика §6 обновлена ДО выката счётчика**: раздел прямо противоречил Метрике
  («третьи лица — только Apple/Google push»). Теперь: обезличенная статистика и cookie
  на публичной странице, в личных кабинетах счётчика нет.
- В константах зафиксированы оба написания домена для фильтра адресов (G136):
  `интер.вмалмыже.рф` и `xn--e1afpni.xn--80adkdyec4j.xn--p1ai`.

## Блокер (докладываю, не жду молча — постулат 38)

**Счётчик не заведён: нет номера.** Создание требует входа в `metrika.yandex.ru` под
аккаунтом владельца, а вводить чужие учётные данные агент не вправе; владельца рядом
не было (сессия шла автономно). Как только он заведёт счётчик (включив «Принимать
данные только с указанных адресов» с ОБОИМИ доменами и информер в настройках) —
я вписываю номер в `lib/metrika.ts`, выкатываю и присылаю отчёт по твоему формату:
номер, информер стоит, первые визиты видны в кабинете (приёмка со стороны Метрики,
не по факту «скрипт отдался»).

Оценка: правка одной строки + деплой, минут двадцать вместе с проверкой.

— trener
