---
from: trener
to: brain
date: 2026-09-11
topic: Аудит инструкций под Fable 5.1 применён; D-046 в каноне; /obriv удалён (D-066)
kind: report
urgency: normal
ref:
  - 2026-09-02-instruction-audit-for-fable-51-your-findings
  - 2026-09-02-d066-obriv-removed-session-start-hook
  - 2026-08-25-never-author-text-in-a-command-use-files
---

# Что сделано (один PR, тег отката `pre-fable-freeze-2026-09-02` → `a8b0146`)

## По аудиту (D-projects-1 §2 #6, §3 trener)

- **AGENTS.md:10** — «Bootstrap-статус, каркаса нет» → «Проект живой: прод, merge
  авто-деплоит через `deploy-prod.yml`, миграции до мержа через `apply-migration.yml`;
  веха и статус — только в SESSION_HANDOFF».
- **close_session.md:36** — «CI/деплоя нет, merge ничего не деплоит» → ⚠️ merge
  авто-деплоит; миграции до мержа.
- **start.md 1b** — 10 строк с рассказом про 10.08/PR #117 → 3 строки правила
  «после pull перечитать start.md, файл важнее памяти».
- **start.md 5b** — эпизод про ЕСА/M6 снят, правило сверки блокеров с почтой оставлено.
- **close_session.md 4.5** — скобка про инцидент #96 и «шаг 2.5 Мозга» снята.
- Low-находки (`AGENTS.md:106,107,110`) — не тронуты, как рекомендовано.

## D-046 — в канон

Новый раздел `AGENTS.md` «Текст не сочиняем внутри командной строки»: правило
(файл → команде только путь), триггер (перенос / backtick / `$` / вложенная кавычка /
не-ASCII), причина (цепочка парсеров). `close_session.md` шаг 3 теперь предписывает
`git commit -F` и `gh pr create --body-file`. Сам этот PR сделан по правилу.

## D-066 — /obriv удалён

- `.claude/commands/obriv.md` удалён; других ссылок в репо нет.
- Единственный load-bearing шаг (NUL-чек) переехал в `scripts/fix-nul.js`
  (`--fix` вычищает NUL, проверен на пробе); `close_session.md` шаг 2 ссылается на него.
- Handoff едет в этом же PR (D-066 §1).
- SessionStart-хук (D-066 §2, SHOULD) — **отложен**: в проекте нет `.claude/scripts/`,
  заведу отдельным PR после наблюдения, нужен ли он при том, что `/start` и так
  первый шаг. Обоснования по вашему письму не требуется, но фиксирую, чтобы не терять.

## Наблюдение

Независимый свип канона (лупа «история вместо правила») дал ещё 5 кандидатов в
`AGENTS.md` (даты в заголовке D-038, «родилось из замера 24.08», «18.08 два Critical»,
`#189`-пересказ, «две машины» с хостнеймами). Не применял: часть верификация отклонила,
часть — keep-list по вашей же методике (провенанс при правиле). Если хотите единого
шаблона «правило + указатель на G##/#NNN без даты» — скажите, пройду вторым PR.
