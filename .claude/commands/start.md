---
description: Старт сессии — синхра своего репо (#032) + двухканальный mailbox-check от brain + чтение SESSION_HANDOFF
---

Выполни старт сессии «trener» строго по шагам (детали — в `AGENTS.md` §📬 Mailbox check). Порядок жёсткий: **сначала синхронизация (шаги 1–2), потом чтение session-памяти (шаг 5)** — pool #032.

1. **Sync свой репо — ПЕРВЫМ:** `git fetch`; если working tree чист и есть отставание — `git checkout main && git pull --ff-only`. Незакоммиченное / не-ff — сообщи и не форсируй. Только после этого можно доверять `SESSION_HANDOFF` и профилю машины. **Синхронизировать можно ТОЛЬКО свой репозиторий** — чужие (включая `../brain_matrica`) не трогать ни `fetch`, ни `pull`, ни `checkout` (мандат владельца 2026-08-04).
2. **Профиль машины (#050):** определи hostname (`$COMPUTERNAME` / `hostname`), прочитай `docs/machines/<hostname>.md` — локальное окружение этой машины (порт/доступ dev-БД, нюанс pnpm, пути, грабли). Разработка идёт с двух машин (`PC40` — работа, `rmz4val` — дом); карта — `docs/machines/README.md`. Нет файла под текущий hostname → новый бокс: завести профиль по мере изучения. Не дублируй содержимое пользователю — используй молча, при расхождении с реальностью правь файл.
3. **Скан входящих — два канала, без синхронизации чужого репо (мандат 08-04):**
   - **Канал А (локально):** прочитай файлы в корне `../brain_matrica/mailboxes/trener/from-brain/*.md` (НЕ `DRAFTS/`, НЕ `ARCHIVE/`) — только чтение, никаких git-команд в чужом репо.
   - **Канал Б (GitHub, без clone/fetch/pull):** листинг того же пути на `main`:
     `gh api "repos/Valstan/brain_matrica/contents/mailboxes/trener/from-brain?ref=main" --jq '.[].name'`.
   - **Набор писем = объединение** двух каналов. Письмо есть только в одном канале → читай оттуда. Одноимённое письмо в обоих каналах различается по содержимому → свежесть определяй по истории **именно этого пути**: незакоммиченная локальная версия — свежее; иначе сравни последний локальный коммит файла с последним коммитом пути на GitHub (`gh api "repos/Valstan/brain_matrica/commits?path=<путь>&per_page=1"`). Порядок не определяется → прочитай обе версии, явно отметь конфликт пользователю, **ничего не перезаписывай**.
   - Свежесть одного репозитория/письма **не переносится** на другие письма или проекты.
4. **Доложи** пользователю сводку писем ДО чтения handoff, в формате:
   ```
   📬 N писем от brain_matrica:
   - [urgency COMPLIANCE] YYYY-MM-DD-slug — тема
   ```
   `urgency: high` — выдели отдельно даже если письмо одно. Для писем без `compliance`: `kind: directive`→MUST, `kind: idea`→SHOULD.
5. **Прочитай** `docs/SESSION_HANDOFF.md` — статус, текущая нитка, следующий шаг. Если `Updated:` старше 14 дней — пометь «handoff может быть неактуален», не доверяй слепо.
5b. **Сверка handoff'а с почтой (#088) — ОБЯЗАТЕЛЬНО:** для каждого блокера/«жду ответа» в «Следующем шаге» и в `PENDING_FOLLOWUPS.md` проверь входящие шага 3 — нет ли письма, которое этот блокер уже снимает. Своя запись «жду» читается первой, звучит терминально и молча побеждает непрочитанное письмо: 26.07.2026 ack на уход с ЕСА пришёл в тот же день, а веха M6 три дня стояла «заблокирована до 09.08» на пустом месте. Расхождение — доложи ДО предложения следующего шага.
6. **Re-триаж отложенного (#033, если ведётся):** если есть `docs/PENDING_FOLLOWUPS.md` — пункты с возрастом `added` > 30 дней ИЛИ `snoozed` ≥ 3× всплыви пользователю (возобновить / переформулировать под текущий код / выкинуть с причиной). Остальные не перечисляй. Нет файла — шаг пропусти.
7. **Сводка main:** `git log --oneline -5` и `git status` — что нового, есть ли незакоммиченное.
8. Кратко предложи пользователю следующий шаг из handoff.

Не начинай правки кода до завершения шагов 1–5b.
