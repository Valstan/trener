---
description: Закрыть сессию — сохранить состояние в SESSION_HANDOFF и запушить всё через PR-flow, чтобы продолжить без потерь на другой машине
---

# /close_session — финализация сессии «trener»

Цель: оставить **explicit pointer** «куда мы шли» в `docs/SESSION_HANDOFF.md` и убедиться, что **всё лежит на `origin`** (handoff + рабочие PR), а brain не тронут — чтобы на другом компьютере хватило `/start` + `git pull --ff-only`, и ничего не потерялось.

## Когда вызывать / когда НЕ вызывать

- ✅ В конце рабочей сессии; перед пересадкой на другую машину; после значимого куска (фича/доки/решение).
- ❌ После короткой консультации без правок (нечего сохранять) — тогда просто скажи, что состояние чистое.

## Шаг 1. Собрать контекст

```bash
git branch --show-current
git status --short
git log --oneline -10
gh pr list --state open
```
Плюс TaskList (если велась) и прочитать текущий `docs/SESSION_HANDOFF.md` — что было в начале.

## Шаг 2. Уточнить у пользователя (если развилка)

- Какая нитка активна для **следующей** сессии и что считать «следующим шагом»?
- Задача завершена? Тогда так и записать (нет подвисшей нитки).
- Появились ли новые решения/ограничения, которых нет в коде/гите (→ в handoff)?

## Шаг 3. Незакоммиченная работа → через PR-flow (НЕ в `main` напрямую, ADR-0002)

Если `git status` непустой помимо handoff/доков:
1. **Гейты** (если трогался код): `corepack pnpm -C web typecheck && corepack pnpm -C web lint` (+ `corepack pnpm -C web build`, если правка существенная).
2. **NUL-чек** (грабля харнесса): `git add -A && git diff --cached --stat` — любой исходник как `Bin` → вычистить NUL и пересохранить UTF-8 (см. `/obriv` шаг 3).
3. Ветка `feat/ fix/ chore/ docs/ refactor/` → коммит → `git push -u origin <ветка>` → `gh pr create` → **показать diff** → дождаться **OK** → `gh pr merge --squash --delete-branch`.
   - На вехе M1–M2 CI/деплоя ещё нет — мерж в `main` ничего не деплоит. Когда появится `deploy-prod.yml` (M3) — добавить сюда варнинг про авто-деплой и #017-поток миграций.

## Шаг 4. Шеринг находки в brain (условный, pool #009)

Был **переносимый** инсайт (новый паттерн / обход бага фреймворка / security-приём)? 3-фильтр: значимость / переносимость / неочевидность.
- Да → создать `mailbox/to-brain/YYYY-MM-DD-slug.md` (`kind: idea|feedback`, `compliance`, `urgency`) **в этом репо**. См. `CLAUDE.md` §📬 и `mailbox/README.md`.
- ❌ Никогда не писать/коммитить в `../brain_matrica/` (read-only). **Тишина = норма** (рутинный фикс/бамп/доменная правка → молчим).

## Шаг 5. Записать `docs/SESSION_HANDOFF.md`

Обновить ключевые секции (абсолютные даты, не «вчера»):
- **Status / Updated / Branch** — что свежее, где работа.
- **Текущая нитка** — над чем шла сессия.
- **Следующий шаг** — конкретный первый шаг следующей сессии (или «задача завершена»).
- **Контекст** — новые решения/ограничения, которых нет в коде/гите.

Закоммитить handoff через тот же PR-flow (ветка `docs/…` или вместе с рабочей веткой).

## Шаг 6. Финальная проверка «ничего не потеряно»

```bash
git status --short        # чисто (кроме намеренно игнорируемого)
git log --oneline -3
gh pr list --state open
git stash list            # пусто — иначе на другой машине это потеря
```
Доложи: ветка, открытые PR, что записано в handoff, что считать следующим шагом.

## Чего НЕ делать

- ❌ Коммитить секреты (`web/.env` — только `.env.example`; #008).
- ❌ Оставлять висящий `git stash` — на другой машине это потеря.
- ❌ Раздувать handoff: это sticky-note (статус/нитка/шаг), а не лог; детали — в гите/PR.
