# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Build / Next.js
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Payload
/media
/build

# Env / secrets (секреты вне репо — #008, /etc/trener/trener.env)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
.nyc_output/

# Claude Code — личные оверрайды прав не коммитим (общий конфиг — .claude/settings.json)
.claude/settings.local.json

# Codex-зеркала CLAUDE.md и .claude/commands — генерируются конвертером на машине.
# В репо не держим: две копии одного текста разъезжаются при первой же правке правил.
AGENTS.md
.agents/
