# Отложенные follow-up'ы (#033)

> Пункты, сознательно отложенные «на потом». `/start` (шаг 7) поднимает пункт, если
> `added` старше 30 дней ИЛИ `snoozed ≥ 3`. Остальные не перечисляет — просто лежат.
> Закрыл пункт — удали строку (история в `git log`).

---

## go-live: реквизиты оператора + уведомление РКН + финализация политики

- **added:** 2026-07-11
- **snoozed:** 0
- **Почему отложено:** фаза закрытого тестирования на вымышленных данных/пользователях
  (решение владельца 2026-07-11). Реальная регистрация оператора и подача уведомления
  РКН — когда приложение обкатано и решено запускаться «вживую».
- **Что сделать при возврате:**
  1. Вписать реальные реквизиты в [`web/src/lib/operator.ts`](../web/src/lib/operator.ts):
     `name`, `legalForm`, `inn`, `address`, `email` (не `example.com`), `phone`,
     опц. `responsiblePerson`.
  2. Подать уведомление в Роскомнадзор (вне кода, портал РКН) → проставить
     `notifiedRknDate` (ISO).
  3. Выставить `OPERATOR_FINALIZED = true` — убирает плашку «Черновик» с `/privacy`,
     политика становится действующей.
  4. Проверить `/privacy` вживую (реквизиты подставились, плашки нет) → задеплоить.
- **⚠️ Не раньше:** публиковать боевую политику (шаг 3) до реального уведомления РКН
  НЕЛЬЗЯ (kickoff §5.1). Фейковые реквизиты в `operator.ts` не вписывать.
