# trener — Session Handoff

> Короткая записка для продолжения работы после `/start`. История изменений — в Git и PR.

**Status:** ✅ «M-ноль» закрыт — 14 из 14 пунктов аудита, всё в проде
**Updated:** 2026-08-01, rmz4val
**Branch:** `main`

## Текущая нить

Сессия 2026-08-01 закрыла остаток «M-ноля» и обе директивы из почты Мозга:

- [#98](https://github.com/Valstan/trener/pull/98) — проверка 4: три живые ссылки `CLAUDE.md` → `AGENTS.md` (Мозг со своей машины их не нашёл — доложено).
- [#99](https://github.com/Valstan/trener/pull/99) — allowlist в vault-клиенте + таймаут fetch; мутационная приёмка #114: 3 красных против старого кода → 9/9. Ответ Мозгу отправлен.
- [#100](https://github.com/Valstan/trener/pull/100) — п.10: будущие матчи (счёт «парой или никак», «Предстоящие/Сыгранные», «Внести результат»). **Миграция batch 13**.
- [#101](https://github.com/Valstan/trener/pull/101) — п.11: лендинг для покупателя («Подключить свою школу», платформа «Тренер», контакт через `NEXT_PUBLIC_SALES_CONTACT`).
- [#102](https://github.com/Valstan/trener/pull/102) — п.6: массовый импорт детей (Excel-список → dry-run → чанки → таблица join-ссылок; email транзитом, 152-ФЗ). **Миграция batch 14** + фикс латентной поломки: `login_tokens.email` required резал `createInviteToken` — кнопка «ссылка-приглашение» в админке была сломана тем же образом.

Наследование хвостов из handoff #97 (по правилу шага 4.5): п.6 ✅ #102 · п.10 ✅ #100 · п.11 ✅ #101 · письмо «список закрыт» ✅ (в PR этой записи) · UX-аудит ➡️ стал следующим шагом · G211-гейт ➡️ ниже · смоук деплоя ➡️ ниже · мутационная приёмка #114: первый гейт прогнан ✅ (#99), как практика ➡️ ниже.

Все деплои зелёные, `/health` 200. Прод-миграции применены по **batch 14** включительно.

## Следующий шаг

**UX-аудит авторизованных экранов после редизайна (#94, #95)** — согласован с Мозгом
как очередь сразу за «M-нолём». Пройти глазами родителя и тренера все табы на
мобильной ширине (светлая/тёмная), выписать несостыковки, починить пачкой.

Хвост «задать контакт продаж» ✅ закрыт: владелец дал каналы 2026-08-01, секция
показывает все пять (`web/src/lib/salesContacts.ts`), переменная
`NEXT_PUBLIC_SALES_CONTACT` за ненадобностью удалена.

## Важный контекст

- Production: `интер.вмалмыже.рф`, сервис `trener` на `:3007`; deploy автоматически после зелёного CI на `main` (сборка — на GitHub Actions, бокс runtime-only). Миграционные PR — по [`docs/migrations.md`](migrations.md): накат ДО мержа, авто-деплой падает на guard (ожидаемо), код доезжает ручным «Deploy to production».
- **Снапшот-дрейф миграций починен** в batch 13: рукописная `branch_operator` (batch 12) шла без `.json`, из-за чего генератор подмешивал её дельту повторно. Письмо-идея Мозгу отправлено. Правило: руками миграции не пишем; если пришлось — следующий `migrate:create` чинит цепочку полным `.json`, чужую дельту из `.ts` вычищаем, верификация обязательна.
- `login_tokens.email` теперь conditional (validate по purpose) — invite-токены живут без email до привязки.
- Юридический go-live остаётся отдельным решением для каждого филиала (закрытое тестирование на вымышленных данных); M10 (S3-галереи) не начинать без решения владельца.

## Хвосты (не блокеры)

- **`access.create` с `Where` у Players / TrainingSessions / Announcements** (G211) — глобально дыру всё ещё затыкают ручные проверки маршрутов; импорт-маршрут валидирует группу против белого списка сам (жёстче). Лечение — гейт в `beforeValidate` по образцу `guardTopicGroup`.
- **Смоук деплоя не рендерит ни одной страницы** — расширить на `/` и `/login` с проверкой маркера в HTML и наличия CSS-бандла.
- **Мутационная приёмка гейтов (#114)** — практика подтверждена на vault-allowlist (#99: красный до правки — зелёный после); применять к каждому следующему гейту, который трогаем.
- Отложенное — в [`docs/PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) (M6 `parked`, go-live 152-ФЗ) — без изменений.

## DEV

- PC40: `D:\GitHubReps\trener`, PostgreSQL 17 на `5433`. Вторая машина — rmz4val (`D:\PROGRAMMING\`, порт 5432); профили в [`docs/machines/`](machines/README.md).
- **rmz4val, новое за 2026-08-01** (детали в профиле): `corepack pnpm -C web …` не работает (только `cd web`); corepack-шим глотает коды выхода — гейты проверять по выводу; knip падает по памяти при параллельных сборках — гонять в одиночку.
- Тестовый владелец после seed: `admin@trener.local` / `devpass1234` (magic-link одноразовые — не «прощупывать» curl'ом перед браузером).
- Основные гейты: `cd web && corepack pnpm lint / typecheck / test -- --run / knip / build`.
