# trener — Session Handoff

> Короткая записка для продолжения работы после `/start`. История изменений — в Git и PR.

**Status:** 🟡 D-029 живёт на проде; [PR #147](https://github.com/Valstan/trener/pull/147) (текст лимита в UI) открыт и ждёт мержа + прод-приёмки
**Updated:** 2026-08-19, rmz4val
**Branch:** `claude/relaxed-agnesi-d4fa31` (PR #147, не смержен)

## Текущая нить

Сессия 2026-08-19 (вечер) — первый хвост демо-режима: дружелюбный текст лимита
витрины не доходил до форм. `demoGuestLimit` кидал голый `Error` → Payload
превращал это в 500 → формы показывали generic «Не удалось отправить».

[PR #147](https://github.com/Valstan/trener/pull/147) — три слоя:
- хук `web/src/hooks/demoGuestLimit.ts` кидает `APIError(DEMO_LIMIT_MESSAGE, 400, undefined, true)`;
- новый хелпер `web/src/lib/apiErrorResponse.ts` пробрасывает **только публичный**
  `APIError` в `{ ok:false, error }` со статусом ошибки; подключён в catch всех
  create-роутов хуканутых коллекций (chat message/topic, announcement, match,
  match/comment, payment, session, question + reply ×2, payment-chat/message);
- 9 форм показывают `data.error`, если он пришёл (в `PaymentMessageForm` вывода
  ошибки не было вовсе — добавлен). QuickPayment/PaymentForm уже умели.

Гейты на rmz4val: `pnpm test` 382/382 (было 347), `typecheck` чисто, `lint` — только
преcуществующие warnings в миграции. CI на момент закрытия сессии ещё крутился.

Предыстория D-029 (сид, изоляция, /demo, reseed-таймер, прод-приёмка) — в PR
[#139](https://github.com/Valstan/trener/pull/139), [#141](https://github.com/Valstan/trener/pull/141),
[#142](https://github.com/Valstan/trener/pull/142), [#143](https://github.com/Valstan/trener/pull/143),
[#144](https://github.com/Valstan/trener/pull/144) и отчёте
`mailbox/to-brain/2026-08-19-demo-mode-live.md`.

## Следующий шаг

1. **Смержить [PR #147](https://github.com/Valstan/trener/pull/147)** (зелёный CI → squash),
   дождаться автодеплоя и принять на проде: демо-тренером отправить **6-е** сообщение
   в чат — вместо «Не удалось отправить» должно быть «В демо можно создать не больше 5…».
   Миграций в PR нет, runbook #017 не задействован.
2. Проверить, что ночной reseed 03:30 прошёл (`journalctl -u trener-demo-reseed.service`)
   и демо-даты сдвинулись — переходит с прошлой сессии, так и не проверено.
3. Оставшиеся хвосты демо-режима (см. ниже), затем — сквозная приёмка бизнес-сценария
   (нить до D-029).

Наследовано из handoff'а 11.08 (для нити приёмки; восстановлено в
[#146](https://github.com/Valstan/trener/pull/146), переходит без изменений):

- Полный сценарий: регистрация → заявка → подтверждение → группа → расписание+RSVP →
  чат → оплата → согласие/отзыв.
- ⚠️ Визуальная приёмка: preview-панель скриншоты не отдаёт («pane is not displayed»),
  обход найден — настоящий Chrome через `claude-in-chrome` скриншотит нормально;
  dev-сервер поднимать preview'ом (порт), смотреть в Chrome. Сессией 18-19.08
  прод-приёмка D-029 успешно шла и через встроенную браузер-панель.
- Кандидаты на нить после приёмки: посещаемость (факт явки — отсутствует как класс,
  только RSVP-намерение); серии тренировок как сущность (массовая правка/отмена);
  живое обновление чата (сейчас только `router.refresh()` после своей отправки).

## Важный контекст

- Production: `интер.вмалмыже.рф`, сервис `trener` на `:3007`; deploy автоматически после зелёного CI на `main`. Миграционные PR — по [`docs/migrations.md`](migrations.md): накат ДО мержа, авто-деплой падает на guard (ожидаемо), код доезжает ручным «Deploy to production».
- Визуальная приёмка: использовать Edge через установленное расширение Codex. Для локального dev на PC40 Edge надёжно открывает `http://127.0.0.1:<порт>`; `localhost` в этой сессии давал таймаут навигации.
- **Красный `main` тихо останавливает прод.** Автодеплой висит на `workflow_run` от CI: чужой красный → deploy `skipped`, без письма.
- **`NEXT_PUBLIC_*` запекаются на CI-сборке** — правкой `/etc/trener/trener.env` не меняются. Поэтому публичные значения (контакты продавца, ID Метрики) живут константами в репо.
- 🆕 **Внешний скрипт отдался с 200 ≠ интеграция работает.** Счётчик Метрики без
  `?id=<номер>` в src грузится, выполняется (`Ya.Metrika2` определён) — и не
  инициализируется: очередь `window.ym.a` не разбирается, ни одного
  `mc.yandex.ru/watch/...` не уходит. Снаружи всё зелено: HTTP 200, ноль ошибок в
  консоли, отрисованный информер — и вечный ноль визитов. **Приёмка интеграции —
  только по подтверждению в панели самого сервиса**, и это условие надо записывать
  в план ДО работ (у нас было записано — потому и поймали за час, а не за месяц).
- 🆕 **Метрика: punycode в фильтр адресов больше не вносится.** Кабинет отвечает
  «Неверный формат URL» на любой `xn--`-адрес (латинский `example.com` в то же поле
  проходит — дело именно в punycode). G136 в этой части устарел; Метрика нормализует
  IDN сама, `page-url` в реальных хитах приходит punycode-хостом и принимается с 200
  при фильтре с одним кириллическим написанием.
- 🆕 **`/start` разворачивается ДО того, как сам синхронизирует репо** — и потому
  может выполняться по отменённой инструкции. Поймано на себе: сессия стартовала с
  отставанием на 15 коммитов и сделала запрещённый `pull` в чужой репо по тексту,
  который накануне отменил PR #117. Лечение — шаг 1b в `/start`: после `pull`
  перечитать файл команды; файл всегда прав.
- **knip локально на rmz4val падает по памяти** — гейт мёртвого кода судить по CI. Заводя хелпер «на будущее», сразу проверяй, что он используется, иначе CI красный.
- **Ветка, отпочкованная от другой ветки, конфликтует после squash-мержа родителя.** Лечится `git format-patch` + новая ветка от `main` (force-push в этом окружении запрещён).
- ⚠️ **`git checkout -- <файл>` сносит несохранённую работу целиком.** За сессию так
  потерялась только что написанная функция: откатывал временную мутацию теста, а
  вместе с ней ушла и правка. Для временных мутаций — точечный обратный `sed`, а не
  откат файла к HEAD.
- 🆕 **Ошибка из Payload-хука доходит до пользователя только двумя условиями сразу.**
  Голый `throw new Error(текст)` в `beforeChange` → 500 без текста. Нужен
  `APIError(текст, 400, undefined, true)`, где четвёртый аргумент `isPublic` —
  обязателен, иначе сообщение всё равно съедается. И этого мало: **кастомные
  Next-роуты `app/(frontend)/**/route.ts` сами `APIError` не сериализуют** — в их
  `catch` нужен явный проброс (`web/src/lib/apiErrorResponse.ts`). Симптом раньше
  времени не виден: сервер «работает», а форма ровно так же говорит «попробуйте ещё раз».
- Юридический контур: реквизиты филиала + подпись договора — технический гейт, а не памятка (D-016).

## Хвосты (не блокеры)

Наследованы с явным статусом каждого (правило #96 — пункт без ярлыка стирать нельзя):

**Демо-режим D-029** (заведены в handoff'е 19.08):

- ✅ **Дружелюбный текст лимита не доходил до UI** — закрыт в
  [PR #147](https://github.com/Valstan/trener/pull/147); **прод-приёмка ещё не сделана**
  (см. «Следующий шаг» п.1).
- ➡️ **У демо-владельца/бухгалтера чат пуст** — branch-admin-скоуп не включает
  group-темы. Преcуществующее поведение, но витрина выглядит голой.
- ➡️ **Ссылка «Администрация» в шапке видна демо-staff**, хотя `/admin` им закрыт.

**Долгие** (тянутся с 09.08):

- ➡️ **`access.create` с `Where` у Players / TrainingSessions / Announcements** (G211) —
  переходит без изменений, за сессию не трогал. Дыру по-прежнему затыкают ручные
  проверки маршрутов; рабочий образец приёма — `guardTopicGroup` и `stampSubscription`.
- ➡️ **Смоук деплоя не рендерит ни одной страницы** — переходит без изменений (сессия
  19.08-вечер его не трогала), и остаётся усиленным доводом с 10.08: деплой отчитался
  зелёным (включая smoke `/health`), пока счётчик на лендинге был мёртв. Расширить на `/` и `/login` с проверкой маркера в HTML и наличия
  CSS-бандла, валидировать поломкой CSS-ссылки на одноразовой ветке (совет Мозга, #113).
- ✅➡️ **Мутационная приёмка гейтов (#114)** — **первый кусок закрыт**: для нового теста
  `metrikaSnippet` мутация проведена (убрал `?id=` — два теста краснеют, вернул —
  зелёные, [#134](https://github.com/Valstan/trener/pull/134)). Пункт переходит:
  три гейта прошлой сессии (`chatTopicScope` + scope-aware `guardTopicGroup`,
  `stampSubscription`, `guardLegalDocument`) мутацией по-прежнему не проверены.
- ➡️ **Отложенное** — в [`docs/PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md): M6 `parked`,
  go-live 152-ФЗ. Пункт «номер счётчика Метрики» ✅ **закрыт и удалён** (D-017 доведён
  до живого счётчика). Остаётся один шаг владельца — **утверждение цен подписки**.

## DEV

- PC40: `D:\GitHubReps\trener`, PostgreSQL 17 на `5433`. Вторая машина — rmz4val (`D:\PROGRAMMING\`, порт 5432); профили в [`docs/machines/`](machines/README.md).
- 🆕 **Порт 3000 на PC40 занимает dev-сервер соседнего проекта** (SabantuyMalmyzh). В `.claude/launch.json` для `web-dev` включён `autoPort` — preview сам берёт свободный порт. Без этого `preview_start` не поднимался вовсе.
- **rmz4val:** `corepack pnpm -C web …` не работает (только `cd web && corepack pnpm …`); corepack-шим глотает коды выхода — гейты судить **по выводу**; `gh` перемежающе таймаутится — длинные цепочки гонять фоном с ретраем; HTTPS-API бокса с этой машины таймаутится, прод-`/health` проверять по SSH через loopback.
- Тестовый владелец после seed: `admin@trener.local` / `devpass1234`.
- Основные гейты: `cd web && corepack pnpm lint / typecheck / test -- --run / knip / build`.
- Тестов на конец сессии: **382**.
- 🆕 **Worktree `.claude/worktrees/<имя>` приезжает без `node_modules`** — первый гейт
  падает с `Cannot find package 'vitest'`. Нужен `cd web && pnpm install` (~10 мин на
  rmz4val, гонять фоном), и только потом test/typecheck/lint.
- 🆕 **Файлы репо — CRLF.** Пакетные правки через `perl -0pi -e` молча не находят
  ничего, если в паттерне `\n`: нужен `\r\n`. Признак — `grep -c` даёт 0 вместо ошибки.
