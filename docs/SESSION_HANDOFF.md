# trener — Session Handoff

> Короткая записка для продолжения работы после `/start`. История изменений — в Git и PR.

**Status:** ✅ Сквозная приёмка бизнес-сценария пройдена целиком; найденная ею утечка живых данных в публичное демо закрыта и проверена на проде; бэклог находок зафиксирован
**Updated:** 2026-08-24, rmz4val
**Branch:** `main`

## Текущая нить

Сессия 24.08 — почта Мозга (1 письмо), правило по браузерам и **сквозная приёмка**
(нить из прошлого handoff'а).

- [#166](https://github.com/Valstan/trener/pull/166) — почта 23.08: `gitleaks` стал
  **required-контекстом** `main` (сверено по API: `["web-quality","gitleaks"]`), моя запись
  в handoff'е утверждала обратное — поправлено. В профиль `rmz4val` записан свип 15.08:
  вместе с задачей бэкапа и каталогом `bin` с диска ушёл **`gh.exe`** (деталь от Мозга, пул #192).
- [#167](https://github.com/Valstan/trener/pull/167) — правило «**Чем смотреть на приложение**»
  в `AGENTS.md` по замеру владельца: скриншот дороже текстового чтения той же страницы
  **в 8–12 раз**, поэтому текст первым; стенд и логи сервера — только встроенная панель;
  картинка, реальные сессии и **вторая роль одновременно** (разные cookie-хранилища) —
  внешний Chrome. Рецепт ушёл Мозгу как idea/suggest.
- **Сквозная приёмка на стенде пройдена целиком** — 24 шага от регистрации до отзыва
  согласия, живьём, двумя ролями одновременно. Чек-лист с наблюдаемыми маркерами —
  [`acceptance-e2e.md`](acceptance-e2e.md), это теперь повторяемая процедура, а не разовый прогон.
- [#168](https://github.com/Valstan/trener/pull/168) — **утечка, которую нашла приёмка**:
  демо-владелец (сессию берёт любой посетитель кнопкой на `/demo`, `roles:['owner']`)
  ЧИТАЛ живую сеть на пяти экранах персонала — email ждущих подтверждения взрослых и ФИО
  детей из саморег-заявок, весь штат сети с email, имена живых филиалов и групп. Запись
  была закрыта ещё #152, дыра осталась на чтении. Первопричина: **#152 прошёл по роутам,
  а страницы (server components) остались на `isOwner`**. Фикс + гейт `demoReadScope.test.ts`
  с мутационной приёмкой; проверено на стенде красным путём и **на проде после деплоя**.
- [#169](https://github.com/Valstan/trener/pull/169) — [`backlog-e2e-2026-08-24.md`](backlog-e2e-2026-08-24.md):
  остальные находки приёмки (код-трейс 8 ног + скептики), приоритеты 1–4, с честной
  пометкой «живьём не воспроизведены». Плюс npm-скрипт `seed:legal` — сид не создаёт
  юр. документы, и на dev они не приезжают миграцией.
- Письма Мозгу: `2026-08-24-gitleaks-required-verified-192-recorded`,
  `2026-08-24-browser-tooling-external-vs-embedded`,
  `2026-08-24-writers-fixed-readers-leaked-demo-owner`.

## Следующий шаг

1. **Приоритет 1 из бэклога — юридический контур** ([`backlog-e2e-2026-08-24.md`](backlog-e2e-2026-08-24.md)):
   начать с гейта согласия. `parentNeedsConsent` вызывается ровно в одном месте
   (`parent/page.tsx:35`), поэтому после отзыва согласия остаются открытыми
   `/parent/schedule`, `/parent/payments`, `/parent/announcements`, `/chat` — вопреки тексту
   окна подтверждения. Рядом: `Consents.access.create: Boolean(user)` (любой авторизованный
   может создать согласие мимо гейта D-016).
   Порядок по каждому пункту — **воспроизвести → красный путь → фикс**: находки бэклога
   получены трейсом кода, а не прогоном.

## Важный контекст

- Production: `интер.вмалмыже.рф`; deploy автоматически после зелёного CI на `main`.
  Миграционные PR — по [`docs/migrations.md`](migrations.md): накат ДО мержа, авто-деплой
  падает на guard (ожидаемо), код доезжает ручным «Deploy to production».
- 🆕 **D-038: инфра-детали бокса в репо не кладём** — правило и исключения в `AGENTS.md`
  («Публичный репозиторий — recon-поверхность»), гейт `scripts/recon-lint.sh` (generic-паттерны,
  без реальных значений). Значения: GitHub secrets (`PROD_SSH_HOST`, `PROD_SSH_USER`,
  `SSH_PRIVATE_KEY`), `EnvironmentFile` бокса (`PORT`, `SECRETS_MANAGER_URL`, секреты),
  локальный `~/.ssh/config`. Handoff — живой документ: порт за прокси и ssh-алиас сюда не писать.
- 🆕 **Required checks на `main` = `web-quality` + `gitleaks`.** Второй добавлен Мозгом
  вечером 23.08 по нашей просьбе (письмо `2026-08-23-gitleaks-now-required-two-finds-pooled-191-192`);
  сверено 24.08 по API: `["web-quality","gitleaks"]`. Красный сканер **блокирует мерж**
  (прежняя запись «не блокирует» была верна только до вечера 23.08). Защита ветки —
  зона Мозга (постулат 34), поэтому новые гейты по-прежнему удобнее класть **шагами внутрь**
  `web-quality` (так сделан recon-lint): это не требует правки required-контекстов на каждый гейт.
- 🆕 **Юниты в `deploy/systemd/` — шаблоны**: `__DEPLOY_USER__`/`__DEPLOY_HOME__` рендерит
  remote-скрипт `deploy-prod.yml`; ручная установка — `sed` из комментария в `trener.service`.
  `PORT` только в `trener.env` бокса: убрать его оттуда = сервис сядет на дефолтный порт Next.
- 🆕 **Vault-клиент — аварийный путь** (`secretsBootstrap`): комната — копия, источник —
  env бокса; `SECRETS_MANAGER_URL` теперь обязателен (лежит в `secrets-token.env`). Мина #171
  не применима; ретраи не заводить — повтор даёт `Restart=on-failure`.
- 🆕 **gitleaks: свои правила в `.gitleaks.toml`** (URL БД с паролем; ENV-форма `*_PASS=`).
  Новый класс секрета → мутация одноразовой веткой (образец: PR #150), а не доверие дефолту.
- 🆕 **`isOwner` vs `isFullOwner` в роутах = граница демо/прод** (#152): демо-владелец имеет
  roles:['owner'], поэтому `isOwner(user)`=true. В server-mediated роутах для скоупа филиала/группы
  использовать `lib/groupScope.staffCanManageGroup` (групповые) или `isFullOwner` (сетевые owner-only);
  `isOwner` оставлять только как роль-гейт (`isCoach||isOwner`) и метку authorRole. Демо → live
  протекает через overrideAccess:true (create-access с Where не фильтрует, G211) — проверять каждый
  новый роут «а что делает демо-владелец». **И afterChange-хуки тоже** (#155): фан-аут с
  ГЛОБАЛЬНЫМ списком получателей (`roles:['owner']` без фильтра демо) шлёт живым, даже когда
  запись скоуплена — `sendPushToUser` режет только адресата-демо. Демо-действие глушить у
  источника по `doc.demoGuest`.
- 🆕 **Паспорт (ADR-0012) работает**: `passport-probe.yml` (dispatch только с `main` —
  сервер пинит субъект `^repo:…:ref:refs/heads/(main|master)$`, с ветки 401 by design).
  Адрес комнаты — secret `VAULT_BASE_URL`. Токен НЕ кладём в `$GITHUB_ENV` (рецепт спеки
  так делает — отдал бы его всем шагам job'а); самоотзыв на `trap … EXIT INT TERM`, иначе
  под `set -e` он недостижим при сбое. Понадобится реальный pull секретов в CI — брать
  токен этим шагом и держать в одном шаге.
- 🆕 **Смоук деплоя — `scripts/smoke.sh <base-url>`**, гоняется по публичному адресу из
  `NEXT_PUBLIC_SERVER_URL`. Новую публичную страницу добавлять туда же строкой `check_page`.
  Правишь скрипт — прогоняй фикстурами (`MODE=good|no-marker|no-css-link|css-404|page-500|health-bad`),
  а не только «на живом»: живой прод зелёный и у декоративного гейта.
- 🆕 **Функцию, которая ставит глобальные переменные, нельзя звать через `$(...)`** —
  подстановка это подоболочка, присваивания не доезжают. Ловится только прогоном с
  заведомо исправным входом: если «сломано всё», подозревай тест, а не предмет.
- 🆕 **Лог CI — тоже recon-канал** (D-038): маска GitHub ловит только точное значение
  секрета, поэтому страница прокси (502/504) с голым хостнеймом проходит мимо неё. Тело
  чужого ответа в лог не печатать — только поля, извлечённые по имени, и обезвреживать `::`
  (stdout раннера парсится на workflow-команды).
- 🆕 **Письма Мозга с `ack: none` всё равно стоит закрывать `ref:`** — три письма 15.08
  висели у него «проект писал, но неясно о чём» до 23.08.
- **Красный `main` тихо останавливает прод.** Автодеплой висит на `workflow_run` от CI:
  чужой красный → deploy `skipped`, без письма.
- **`NEXT_PUBLIC_*` запекаются на CI-сборке** — правкой env бокса не меняются; публичные
  значения (контакты продавца, ID Метрики) живут константами в репо.
- **Внешний скрипт отдался с 200 ≠ интеграция работает** (Метрика без `?id=`): приёмка
  интеграции — только по подтверждению в панели самого сервиса, условие писать в план ДО работ.
- **Метрика: punycode в фильтр адресов не вносится** («Неверный формат URL»); G136 в этой
  части устарел.
- **`/start` разворачивается ДО синхры репо** — после `pull` перечитать файл команды (шаг 1b).
- **knip локально на rmz4val падает по памяти** — гейт мёртвого кода судить по CI.
- **Ветка от ветки конфликтует после squash-мержа родителя** — `git format-patch` + новая
  ветка от `main`.
- ⚠️ **`git checkout -- <файл>` сносит несохранённую работу целиком** — для временных
  мутаций точечный обратный `sed`.
- Юридический контур: реквизиты филиала + подпись договора — технический гейт (D-016).

- 🆕 **Фикс писателей не закрывает читателей** (#168). Граница демо/прод живёт в двух
  слоях: роуты (запись) и страницы/выборки (чтение). #152 прошёл по первому — второй
  остался открыт. Правило: сетевой охват решает **только `isFullOwner`**; `isOwner`
  допустим как роль-гейт (`if (!isOwner(user)) redirect('/home')`), но не как «показать
  всё». `overrideAccess: true` — это заявка на ручной скоуп рядом, в том же месте.
  Автогейт — `web/src/access/demoReadScope.test.ts` (читает исходники пяти экранов:
  server component юнит-тестом без БД не поднять).
- 🆕 **Приёмку судим по наблюдаемому эффекту у конечного потребителя** — и по обеим
  сторонам границы: сначала сценарий целиком нужной ролью, потом **те же экраны
  демо-гостем**. Утечку #168 не нашли ни ревью, ни сканер, ни аудит-свип 32 агентами —
  нашёл проход демо-владельцем по тем же URL.
- 🆕 **Чем смотреть на приложение** — раздел в `AGENTS.md`: текст первым (скриншот дороже
  в 8–12 раз); dev-сервер и логи сервера только через встроенную панель (в dev оттуда
  берётся ссылка magic-link, иначе регистрацию на стенде не пройти); картинка и реальные
  сессии — внешний Chrome; **вторая роль = второй браузер** (разные cookie-хранилища);
  `filter=interactive` у чтения страницы пропускает часть кнопок — перечитывать `filter=all`.
- 🆕 **Юр. документы на стенде** — `cd web && corepack pnpm seed:legal` (сид их не создаёт,
  а на dev миграции не накатываются: drizzle-push). Без них весь юридический контур мёртв.

## Хвосты (не блокеры)

Наследованы из handoff'а 23/24.08 с явным статусом каждого (правило #96 — пункт без
ярлыка стирать нельзя). Закрытые в прошлой сессии и уже помеченные ✅ не повторяю:
ночной reseed, текст лимита витрины, смоук по содержимому, строка про границу демо/прод
в `AGENTS.md`, демо-гард на `coach/import`, пилот паспорта, аудит-свип демо→живое.

- ✅ **Сквозная приёмка бизнес-сценария** — пройдена 24.08 целиком, оформлена как
  повторяемый чек-лист [`acceptance-e2e.md`](acceptance-e2e.md) с отдельным блоком
  «красный путь демо-владельца» (именно он и поймал утечку #168).
- ➡️ **Бэклог приёмки** — [`backlog-e2e-2026-08-24.md`](backlog-e2e-2026-08-24.md):
  19 пунктов приоритетов 1–4 плюс 44 минора. Живьём не воспроизведены.
- ➡️ **У демо-владельца/бухгалтера чат пуст** (branch-admin-скоуп не включает
  group-темы — преcуществующее, но витринно голо). Смежное из бэклога: админ филиала
  создаёт тему и получает 404 при её открытии.
- ➡️ **Ссылка «Администрация» в шапке видна демо-staff**, хотя `/admin` им закрыт.
- ➡️ **`access.create` с `Where` у Players / TrainingSessions / Announcements** (G211) —
  без изменений; дыру затыкают ручные проверки маршрутов.
- ✅➡️ **Мутационная приёмка гейтов (#114)** — новый гейт `demoReadScope.test.ts` принят
  красным путём (возврат `isOwner` роняет 2 теста). Переходит: три гейта позапрошлой
  сессии (`chatTopicScope` + scope-aware `guardTopicGroup`, `stampSubscription`,
  `guardLegalDocument`) мутацией по-прежнему не проверены.
- ➡️ **Мутационно проверить `staffCanManageGroup`** одноразовой веткой на проде (#114) —
  без изменений с 23.08.
- ➡️ **Отложенное** — [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md): M6 `parked`,
  go-live 152-ФЗ, цены подписки. ⚠️ Пункт **go-live** снова всплыл на `/start` 24.08
  (44 дня): техчасть сделана (D-016), остались шаги владельца по каждой школе; решение
  владельца по переформулировке в «чек-лист ввода реальной школы» так и не получено,
  пункт не менял.
- ➡️ **Апгрейд задачи `trener-backup-pull` до S4U** на rmz4val (сейчас `Interactive`) —
  одна команда из админ-консоли, записана в профиле машины. Стяжка живая: `_pull.log`
  от 24.08 — `pull ok, 35 dump(s) staged`.
- ➡️ **Остаток D-038 low**: комментарий в `deploy-prod.yml:7` «версия рантайма на боксе» —
  убрать привязку к боксу при следующей правке workflow.

## DEV

- PC40: `D:\GitHubReps\trener`, PostgreSQL 17 на `5433`. Вторая машина — rmz4val (`D:\PROGRAMMING\`, порт 5432); профили в [`docs/machines/`](machines/README.md).
- **Порт 3000 на PC40 занимает dev-сервер соседнего проекта** — в `.claude/launch.json` для `web-dev` включён `autoPort`.
- **rmz4val:** `corepack pnpm -C web …` не работает в Git Bash (только `cd web && corepack pnpm …`), но **через preview-лончер эта форма отрабатывает** — конфиг `web-dev` в `.claude/launch.json` поднялся 24.08 как есть; corepack-шим глотает коды выхода — гейты судить **по выводу**; `gh` перемежающе таймаутится — длинные цепочки гонять фоном с ретраем; **агентская сессия не admin** (S4U-задачи, `Start-Service` — через обходы из профиля); на `/start` проверять свежесть `D:\YandexDisk\Backups\trener\_pull.log`.
- Тестовый владелец после seed: `admin@trener.local` / `devpass1234`.
- Основные гейты: `cd web && corepack pnpm lint / typecheck / test -- --run / knip / build`; в корне — `bash scripts/recon-lint.sh`.
- Тестов на конец сессии: **402** (после #168 — гейт `demoReadScope`).
- **Скриншоты встроенной панели на этой машине не работают** («pane is not displayed»),
  поэтому клик по координатам там недоступен (координаты требуют скриншота) — только по
  `ref`; визуальная часть — внешний Chrome.
