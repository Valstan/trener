# trener — Session Handoff

> Короткая записка для продолжения работы после `/start`. История изменений — в Git и PR.

**Status:** ✅ D-038 закрыт (инфра вне репо + recon-lint); gitleaks дополнен; **дыра изоляции демо→живое закрыта (14 роутов, #152)**; прод на новых юнитах; отчёт Мозгу отправлен
**Updated:** 2026-08-23, rmz4val
**Branch:** `main`

## Текущая нить

Сессия 2026-08-23 — почта Мозга (4 письма) и мандат **D-038** «публичный репозиторий —
recon-поверхность»:

- [#149](https://github.com/Valstan/trener/pull/149) — аудит (4 лензы + скептики + критик)
  и чистка: хост/пользователь бокса → GitHub secrets `PROD_SSH_HOST`/`PROD_SSH_USER`;
  юниты с плейсхолдерами `__DEPLOY_USER__`/`__DEPLOY_HOME__` (рендер remote-скриптом
  деплоя); `PORT` и `SECRETS_MANAGER_URL` → `EnvironmentFile` бокса (добавлены до мержа);
  дефолт адреса менеджера из кода убран; тексты доков/комментариев без хостера, «Бокса N»,
  соседей, ssh-алиаса, портов. Предохранитель — раздел в `AGENTS.md` + `scripts/recon-lint.sh`
  шагом внутри required-джоба CI (мутационная приёмка пройдена). Попутно G234 амендмент-2
  (KEEP_ALIVE 60000, ложный минус про SIGTERM исправлен).
- [#151](https://github.com/Valstan/trener/pull/151) — `.gitleaks.toml`: мутация на своих
  классах показала, что дефолт пропускает `DATABASE_URL` с паролем и `SMTP_PASS=…`;
  два правила + allowlist dev-плейсхолдеров; приёмка 7/7 красных, разрешённые формы зелёные.
- [#147](https://github.com/Valstan/trener/pull/147) — дружелюбный текст лимита витрины
  (хвост 19.08) — смержен.
- [#152](https://github.com/Valstan/trener/pull/152) — **безопасность D-029/#166**: демо-владелец
  (roles:['owner']) через `isOwner` в 14 server-mediated роутах дотягивался до ЖИВЫХ данных
  (тренировки/матчи/объявления/темы чата в любой группе → пуши живым; подпись договора за
  чужой филиал; staff-invite = login-письмо на любой адрес из витрины; approve/delete живых
  заявок; ПДн живых родителей). Фикс — лестница `lib/groupScope.staffCanManageGroup` +
  `isOwner→isFullOwner`; проверено красным путём на стенде (демо→живое 403/400, демо→демо 200).
- Деплой `fde527f`/`1005a83`/`6396eef` прошли новым remote-скриптом: юниты отрендерены,
  `/health` 200, `/demo` 200, cron-юнит с `${PORT:?}` — `ok:true`.
- Письмо Мозгу `mailbox/to-brain/2026-08-23-d038-done-vault-no-mine-scanner-own-classes.md`
  (ref на 7 писем: D-038, три поправки, vault #171, демо-принят, три письма 15.08).
- Вне репо: на rmz4val offsite pull-стяжка бэкапов была **мертва с 15.08** (Scheduled Task
  и `bin\` пропали) — восстановлена, `pull ok`; детали в `docs/machines/rmz4val.md`.

## Следующий шаг

1. **Прод-приёмка текста лимита** (#147 смержен): демо-тренером 6-е сообщение в чат → в форме
   виден текст «В демо можно создать не больше 5…», а не generic 500.
2. **Пилот паспорта (ADR-0012)** — гейт волны 2 у КАРМАНа, строка реестра для trener уже
   заведена 15.08. Объём: `permissions: id-token: write` + шаг `POST /api/secrets/session`
   с `audience=karman-vault` в workflow, где CI ходит в комнату; спека —
   `karman/docs/passport-server.md` **только на GitHub** (локальная копия karman отстала,
   читать через `gh api … | base64 -d`). Мозг ждёт первый зелёный прогон.
3. **Смоук деплоя контент-маркером** — `/`, `/login`, `/demo` (совет Мозга 08-19: `/demo`
   теперь самая посещаемая страница у незнакомых) + наличие CSS-бандла; валидировать
   поломкой на одноразовой ветке (#113). Сейчас smoke = только `/health` 200.
4. Сквозная приёмка бизнес-сценария (нить до D-029).

Наследовано из handoff'а 11.08 (для нити приёмки):

- Полный сценарий: регистрация → заявка → подтверждение → группа → расписание+RSVP →
  чат → оплата → согласие/отзыв.
- ⚠️ Визуальная приёмка: preview-панель скриншоты не отдаёт («pane is not displayed»),
  обход найден — настоящий Chrome через `claude-in-chrome` скриншотит нормально;
  dev-сервер поднимать preview'ом (порт), смотреть в Chrome. Сессией 18-19.08
  прод-приёмка D-029 успешно шла и через встроенную браузер-панель.
- Кандидаты на нить после приёмки: посещаемость (факт явки — отсутствует как класс,
  только RSVP-намерение); серии тренировок как сущность (массовая правка/отмена);
  живое обновление чата (сейчас только `router.refresh()` после своей отправки).

## Важный контекст

- Production: `интер.вмалмыже.рф`; deploy автоматически после зелёного CI на `main`.
  Миграционные PR — по [`docs/migrations.md`](migrations.md): накат ДО мержа, авто-деплой
  падает на guard (ожидаемо), код доезжает ручным «Deploy to production».
- 🆕 **D-038: инфра-детали бокса в репо не кладём** — правило и исключения в `AGENTS.md`
  («Публичный репозиторий — recon-поверхность»), гейт `scripts/recon-lint.sh` (generic-паттерны,
  без реальных значений). Значения: GitHub secrets (`PROD_SSH_HOST`, `PROD_SSH_USER`,
  `SSH_PRIVATE_KEY`), `EnvironmentFile` бокса (`PORT`, `SECRETS_MANAGER_URL`, секреты),
  локальный `~/.ssh/config`. Handoff — живой документ: порт за прокси и ssh-алиас сюда не писать.
- 🆕 **Required checks на `main` = только `web-quality`** (снято Мозгом 17.08). Check
  `gitleaks` (`secret-scan.yml`) **не блокирует мерж** — попросил Мозга добавить письмом
  23.08 (защита ветки — его зона, постулат 34). Новые гейты поэтому кладём **шагами внутрь**
  `web-quality` (так сделан recon-lint), а не отдельными джобами.
- 🆕 **Юниты в `deploy/systemd/` — шаблоны**: `__DEPLOY_USER__`/`__DEPLOY_HOME__` рендерит
  remote-скрипт `deploy-prod.yml`; ручная установка — `sed` из комментария в `trener.service`.
  `PORT` только в `trener.env` бокса: убрать его оттуда = сервис сядет на дефолтный порт Next.
- 🆕 **Vault-клиент — аварийный путь** (`secretsBootstrap`): комната — копия, источник —
  env бокса; `SECRETS_MANAGER_URL` теперь обязателен (лежит в `secrets-token.env`). Мина #171
  не применима; ретраи не заводить — повтор даёт `Restart=on-failure`.
- 🆕 **gitleaks: свои правила в `.gitleaks.toml`** (URL БД с паролем; ENV-форма `*_PASS=`).
  Новый класс секрета → мутация одноразовой веткой (образец: PR #150), а не доверие дефолту.
- 🆕 **`isOwner` vs `isFullOwner` в роутах = граница демо/прод** (#152): демо-владелец имеет
  roles:['owner'], поэтому `isOwner(user)`=true. В server-mediated роутах для скоупа филиала/группы
  использовать `lib/groupScope.staffCanManageGroup` (групповые) или `isFullOwner` (сетевые owner-only);
  `isOwner` оставлять только как роль-гейт (`isCoach||isOwner`) и метку authorRole. Демо → live
  протекает через overrideAccess:true (create-access с Where не фильтрует, G211) — проверять каждый
  новый роут «а что делает демо-владелец». **И afterChange-хуки тоже** (#155): фан-аут с
  ГЛОБАЛЬНЫМ списком получателей (`roles:['owner']` без фильтра демо) шлёт живым, даже когда
  запись скоуплена — `sendPushToUser` режет только адресата-демо. Демо-действие глушить у
  источника по `doc.demoGuest`.
- 🆕 **Письма Мозга с `ack: none` всё равно стоит закрывать `ref:`** — три письма 15.08
  висели у него «проект писал, но неясно о чём» до 23.08.
- **Красный `main` тихо останавливает прод.** Автодеплой висит на `workflow_run` от CI:
  чужой красный → deploy `skipped`, без письма.
- **`NEXT_PUBLIC_*` запекаются на CI-сборке** — правкой env бокса не меняются; публичные
  значения (контакты продавца, ID Метрики) живут константами в репо.
- **Внешний скрипт отдался с 200 ≠ интеграция работает** (Метрика без `?id=`): приёмка
  интеграции — только по подтверждению в панели самого сервиса, условие писать в план ДО работ.
- **Метрика: punycode в фильтр адресов не вносится** («Неверный формат URL»); G136 в этой
  части устарел.
- **`/start` разворачивается ДО синхры репо** — после `pull` перечитать файл команды (шаг 1b).
- **knip локально на rmz4val падает по памяти** — гейт мёртвого кода судить по CI.
- **Ветка от ветки конфликтует после squash-мержа родителя** — `git format-patch` + новая
  ветка от `main`.
- ⚠️ **`git checkout -- <файл>` сносит несохранённую работу целиком** — для временных
  мутаций точечный обратный `sed`.
- Юридический контур: реквизиты филиала + подпись договора — технический гейт (D-016).

## Хвосты (не блокеры)

Наследованы из handoff'а 19.08 с явным статусом каждого (правило #96 — пункт без
ярлыка стирать нельзя):

- ✅ **Ночной reseed 03:30 прошёл, демо-даты сдвинулись** — проверено 23.08: три ночи
  подряд `ok:true`, тренировки демо-филиала на 24–30.08; плюс ручной прогон после деплоя.
- ✅ **Дружелюбный текст лимита не доходит до UI** — [#147](https://github.com/Valstan/trener/pull/147)
  смержен 23.08; прод-приёмка по наблюдаемому эффекту (6-е сообщение → текст) остаётся ручным шагом.
- ➡️ **У демо-владельца/бухгалтера чат пуст** (branch-admin-скоуп не включает
  group-темы — преcуществующее, но витринно голо).
- ➡️ **Ссылка «Администрация» в шапке видна демо-staff**, хотя `/admin` им закрыт.
- ➡️ **`access.create` с `Where` у Players / TrainingSessions / Announcements** (G211) —
  без изменений; дыру затыкают ручные проверки маршрутов; образец — `guardTopicGroup`,
  `stampSubscription`.
- ➡️ **Смоук деплоя не рендерит ни одной страницы** — переходит как шаг 3 (с `/demo`).
- ✅➡️ **Мутационная приёмка гейтов (#114)** — за сессию два новых гейта приняты красным
  путём (recon-lint: 6 категорий; gitleaks: свои классы, #150). Переходит: три гейта
  прошлой сессии (`chatTopicScope` + scope-aware `guardTopicGroup`, `stampSubscription`,
  `guardLegalDocument`) мутацией по-прежнему не проверены.
- ➡️ **Отложенное** — [`docs/PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md): M6 `parked`,
  go-live 152-ФЗ, цены подписки (шаг владельца). ⚠️ Пункт **go-live** всплыл на `/start`
  23.08 (43 дня > 30): техчасть сделана (D-016), остались шаги владельца по каждой школе —
  предложено переформулировать в «чек-лист ввода реальной школы»; решение владельца не
  получено, пункт не менял.
- ➡️ **Апгрейд задачи `trener-backup-pull` до S4U** на rmz4val (сейчас `Interactive`) —
  одна команда из админ-консоли, записана в профиле машины.
- ✅ **Границу демо/прод — в чек-лист релиза** (хвост письма Мозга 08-19, #166) — строка
  добавлена в `AGENTS.md` («Граница демо/прод») закрывающим PR 23.08.
- ➡️ **Остаток D-038 low** (критик полноты после мержа #149): комментарий в
  `deploy-prod.yml:7` «версия рантайма на боксе» (убрать привязку к боксу при следующей
  правке workflow — отдельно не трогал, чтобы не гонять деплой ради комментария).
- ✅ **Belt-and-suspenders демо-гард на `coach/import`** (спавн-таск) — закрыт
  [#154](https://github.com/Valstan/trener/pull/154) уже ПОСЛЕ закрывающего #153: явный
  `isDemo→400` в начале `coach/import/run/route.ts` + route-уровневый юнит (демо → 400
  `errorCode=demo`, `sendPlayerJoinEmail` не вызван). Мотив — не «сегодня дыра», а то, что
  защита держалась на одном D-016-гейте: засеют реквизиты демо-филиала с подписью — гейт
  откроется, а `email` в CSV attacker-controlled (спам-вектор из публичной витрины).
  **Парный `auth/invite` проверен — изменений НЕ требует** (решение, из кода не выводимое):
  писем не шлёт (отдаёт `joinUrl` вызывающему), а `readPlayers` скоупит демо-владельца
  демо-филиалом через `isFullOwner` — **не** зависит от реквизитов, поэтому тот же триггер
  его не разоблачает. Разбор — в теле #154.
- ✅ **Полный аудит-свип демо→живое (20 роутов, 32 агента)** после #152 подтвердил покрытие;
  один хвост в **хуке** (не в скоупе роута) — `fanOutPaymentMessage` слал пуш живому владельцу
  на демо-реплику — закрыт в [#155](https://github.com/Valstan/trener/pull/155).
- ➡️ **Мутационно проверить `staffCanManageGroup`** одноразовой веткой на проде (#114): дыра
  #152 закрыта и проверена на стенде (и на проде: legal/sign 400, parent-search 403), но
  подсадной красный путь по классу «демо→живое» отдельной веткой на проде не гонялся.

## DEV

- PC40: `D:\GitHubReps\trener`, PostgreSQL 17 на `5433`. Вторая машина — rmz4val (`D:\PROGRAMMING\`, порт 5432); профили в [`docs/machines/`](machines/README.md).
- **Порт 3000 на PC40 занимает dev-сервер соседнего проекта** — в `.claude/launch.json` для `web-dev` включён `autoPort`.
- **rmz4val:** `corepack pnpm -C web …` не работает (только `cd web && corepack pnpm …`); corepack-шим глотает коды выхода — гейты судить **по выводу**; `gh` перемежающе таймаутится — длинные цепочки гонять фоном с ретраем; **агентская сессия не admin** (S4U-задачи, `Start-Service` — через обходы из профиля); на `/start` проверять свежесть `D:\YandexDisk\Backups\trener\_pull.log`.
- Тестовый владелец после seed: `admin@trener.local` / `devpass1234`.
- Основные гейты: `cd web && corepack pnpm lint / typecheck / test -- --run / knip / build`; в корне — `bash scripts/recon-lint.sh`.
- Тестов на конец сессии: **392** (после #147/#152/#154/#155; прогнано на смерженном `main`).
