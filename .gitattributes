# Shell-скрипты и systemd-юниты — всегда LF, даже при checkout на Windows-машине (#050):
# CRLF в shebang ломает интерпретатор на боксе, CRLF в unit-файле — парсинг systemd.
*.sh      text eol=lf
*.service text eol=lf
*.timer   text eol=lf
