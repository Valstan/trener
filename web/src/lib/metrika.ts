// Яндекс.Метрика (D-017, решение владельца 09.08): счётчик + ВИДИМЫЙ информер
// посещаемости в подвале — на публичном лендинге и ТОЛЬКО там.
//
// Почему константа в репо, а не env: значение публичное (номер счётчика виден в
// исходнике страницы любому), а `NEXT_PUBLIC_*` у нас запекается на CI-сборке —
// правкой /etc/trener/trener.env оно не меняется, и расхождение CI↔прод ломает
// счётчик молча (эмпирика письма 2026-08-02). Прецедент: `NEXT_PUBLIC_SALES_CONTACT`
// удалена 01.08 ровно по этой причине — публичные значения живут рядом с
// salesContacts.ts и ecosystem.ts.
//
// Счётчик заведён 2026-08-10 в кабинете metrika.yandex.ru (аккаунт владельца,
// браузерная сессия — разрешение владельца от 10.08). Настройки счётчика:
//   • «Принимать данные только с указанных адресов» — включено, адрес один:
//     `интер.вмалмыже.рф`. ⚠️ ПОПРАВКА К G136: внести ВТОРЫМ адресом punycode
//     (`xn--e1afpni.xn--80adkdyec4j.xn--p1ai`) в 2026 УЖЕ НЕЛЬЗЯ — кабинет
//     отвечает «Неверный формат URL» на любой `xn--`-адрес (проверено: латиница
//     `example.com` в том же поле принимается, значит дело именно в punycode).
//     Метрика нормализует IDN сама, кириллического написания достаточно.
//   • Вебвизор/карта скроллинга/аналитика форм — ВЫКЛЮЧЕНЫ: самая тяжёлая часть
//     скрипта, а на лендинге записи сессий не нужны (решение владельца 10.08).
//   • Информер — включён, 88×31, метрика на бейдже «Посетители», тип
//     «Расширенный» (по наведению — панель с просмотрами/визитами/посетителями
//     за сегодня, вчера, неделю и месяц). Вид бейджа меняется в кабинете:
//     Настройки → Счетчик → Дополнительные настройки → Информер → «Настроить».
//
// null → скрипт и информер не рендерятся вовсе.
export const METRIKA_COUNTER_ID: number | null = 111457538

// Тело inline-скрипта счётчика. Вынесено из компонента в чистую функцию ровно
// ради теста рядом: этот сниппет ломается МОЛЧА — страница рендерится, tag.js
// отдаётся с 200, а просмотры не отправляются и в кабинете вечный ноль.
//
// ⚠️ `?id=<counter>` в src обязателен. Без него tag.js грузится, но очередь
// `ym.a` остаётся неразобранной: счётчик не инициализируется и ни одного
// запроса `mc.yandex.ru/watch/<counter>` не уходит. Поймано на проде 10.08 —
// первая версия D-017 (PR #127) писала src без параметра, потому что счётчика
// ещё не существовало и проверить было не на чем. Диагноз подтверждён
// эмпирически: догрузка `tag.js?id=...` в ту же страницу немедленно разобрала
// очередь и отправила два `watch/...` с 200.
export const metrikaSnippet = (id: number): string =>
  `(function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
m[i].l=1*new Date();for(var j=0;j<document.scripts.length;j++){if(document.scripts[j].src===r){return;}}
k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
(window,document,'script','https://mc.yandex.ru/metrika/tag.js?id=${id}','ym');
ym(${id},'init',{ssr:true,accurateTrackBounce:true,trackLinks:true});`
