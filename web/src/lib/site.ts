// Идентичность публичного сайта в одном месте.
//
// Раньше SITE_URL и SITE_DESCRIPTION жили константами внутри layout.tsx. Как только
// их понадобилось знать ещё и лендингу (JSON-LD, D-088), копирование значений создало
// бы два источника правды: разметка для нейросетей разошлась бы с <meta> на той же
// странице при первой же правке текста — и разошлась бы МОЛЧА.
//
// NEXT_PUBLIC_SERVER_URL запекается на CI-сборке (правкой env бокса не меняется),
// поэтому дефолт здесь обязан быть верным прод-адресом, а не localhost.

export const SITE_URL = process.env.NEXT_PUBLIC_SERVER_URL || 'https://интер.вмалмыже.рф'

export const SITE_DESCRIPTION =
  'Расписание тренировок, уведомления об изменениях с подтверждением от родителей и учёт оплат абонементов — для детской спортивной школы.'
